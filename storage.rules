rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for sound playback
    // Write/delete ONLY via Cloud Functions (using Admin SDK)
    match /sounds/{allPaths=**} {
      allow read: if true;
      allow write, create, update, delete: if false;
    }
  }
}
